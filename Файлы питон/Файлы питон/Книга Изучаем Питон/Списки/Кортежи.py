# В языке Python значения, которые
# не могут изменяться, называются неизменяемыми (immutable), а неизменяемый
# список называется кортежем

dimensions = (200,300)
print(dimensions[0])

# К кортежам применяються теже методы что и к спискам